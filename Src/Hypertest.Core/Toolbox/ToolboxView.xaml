<UserControl x:Class="Hypertest.Core.Toolbox.ToolboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Hypertest.Core.Converters"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:ScenarioToObservableCollectionConverter x:Key="TestConverter" />
    </UserControl.Resources>
    <Grid>
        <!--<TreeView Name="TreeView1" ItemsSource="{Binding ., Mode=OneWay, Converter={StaticResource ResourceKey=TestConverter}}" >
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children, Mode=TwoWay}">
                    <TextBlock Text="{Binding Name}" Margin="4,0,4,0" ToolTip="{Binding Description}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>-->
    </Grid>
</UserControl>