<UserControl x:Class="Hypertest.Core.GUI.TestWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:gui="clr-namespace:Hypertest.Core.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <gui:TestTree x:Name="testTree1" Grid.Column="0"></gui:TestTree>
        <ContentControl Grid.Column="1" DataContext="{Binding ElementName=testTree1, Path=TreeView.SelectedItem, UpdateSourceTrigger=PropertyChanged}" Content="{Binding Path=Control}" />
    </Grid>
</UserControl>
